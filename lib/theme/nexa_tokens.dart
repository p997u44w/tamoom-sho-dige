import 'package:flutter/material.dart';

/// توکن‌های طراحی Nexa — پالت «شیشه‌ی مایع روی فضای تیره».
/// رنگ‌های اصلی/ثانویه از تمِ هر مدرسه میان (قابل تغییر از پنل ادمین)،
/// این فایل فقط مقادیر ثابت (پس‌زمینه، شیشه، متن) رو نگه می‌داره.
class NexaTokens {
  static const bgDeep = Color(0xFF0A0E17);
  static const bgDeep2 = Color(0xFF121A2E);

  static const amberAccent = Color(0xFFFFB020);

  static Color glassFill = Colors.white.withOpacity(0.10);
  static Color glassBorder = Colors.white.withOpacity(0.20);

  static const textPrimary = Color(0xFFF5F7FA);
  static const textMuted = Color(0xFF93A0B8);

  static const radiusLg = 28.0;
  static const radiusMd = 20.0;
}
